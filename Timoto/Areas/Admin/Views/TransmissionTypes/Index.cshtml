@model List<Timoto.Models.TransmissionType>
@{
    int count = 0;
}
<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Fuel Types</h4>
                <a asp-action="Create" class="btn btn-success"> Create </a>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>No</th>
                                <th>Name</th>
                                <th>Cars Count</th>
                                <th>Created Date</th>
                                <th>Settings</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var transmission in Model)
                            {
                                count++;
                                <tr>
                                    <td>@count</td>
                                    <td>@transmission.Name</td>
                                    <td>@(transmission.Cars?.Count ?? 0)</td>
                                    <td>@transmission.CreatedAt.ToString("d MMMM yyyy HH:mm")</td>
                                    <td>
                                        <a asp-action="Update" asp-route-id="@transmission.Id" class="btn btn-warning">
                                            <i class="mdi mdi-pencil"></i>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@transmission.Id" class="btn btn-danger">
                                            <i class="mdi mdi-delete"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
